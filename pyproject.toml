[tool.poetry]
name = "portalWithDJ"
version = "0.1.0"
description = "A Django-based portal"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.13"
django = "^4.2"
gunicorn = "^22.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
